# Agent feedback loop · 10 seconds

Mode: auto. User explicitly requested a generic open-source technical animation skill and a 10s no-voice demo, using Agent mechanics as an example. No external publishing, brand reuse or paid asset calls authorised.

Audience: technically curious viewers who recognise a code test. One takeaway: a tool's result changes the next decision; the system validates and stops when the goal is met. Not a complete Agent architecture course.

Format: 1920×1080, 60 fps, exactly 600 frames, fully silent (not just muted speech). Original comic-lab graphics. References express an aesthetic direction; this is not a branded clone.

Fixed illustrative task: make add(2,3) produce 5. First implementation subtracts and fails. Failure is returned to the model context. The next selected tool action applies a patch. Another test succeeds; success returns before the task is marked done.

Simplifications: no actual model call; no hidden chain-of-thought claim; no live terminal recording; dispatcher/scheduler not separately drawn; tool execution can fail in real applications; long-term memory, parallel agents, safety budget and human approval omitted to keep one mechanism readable.
