# v0.8 mechanism-first release

Approved scope: implement the previously reviewed first three phases. Preserve topic-specific image creation and v0.7 operations/audio. No customer artwork or credentials in the public release.

## Execution
1. Reproduce geometry-only final acceptance; add failing tests for typed effects, dependencies, missing witnesses and source/handoff gates.
2. Add mechanism_core.py for pre-asset validation, typed conditions, finite branches and deterministic effects. Bind outcomes to the existing compiler rather than duplicating choreography.
3. Add action_requirements.py; derive ports, parts and states from operations before images exist. Do not assign fixed subjects.
4. Add visual_evidence.py; capture actual DOM state, pre/contact/commit/post, reduced-text and mechanism-only views. Pending review cannot approve itself.
5. Add academic_delivery.py; evidence ledger and fingerprinted teacher sign-off for customer handoff, separate from internal render/review.
6. Integrate CLI, renderer, docs and regression fixtures. Keep legacy exports explicitly unqualified; final v0.8 export must pass new gates.
7. Run regression, browser/export, mutation checks, syntax compatibility and integrity checks. Package with exact tests and limitations. Attempt authorized repository publication only with real available tools.

## Non-goals
General concurrent DAG, arbitrary physics, automatic segmentation, guaranteed pedagogy, TTS and beat detection are not implemented. Interactive parameter lessons must not be disguised playback controls.

## Release result

Implemented this iteration: typed finite events, action-derived image requirements,
backend/effect agreement, event-level ablation and rendered witnesses, reviewed-media
export, and separate academic source/instructor handoff. General physics, parameter-driven
teaching controls, general camera planning and parallel DAG scheduling are not implemented.

New-topic real-image production, weaker-model cold-start and learning outcome experiments
remain not_run. Synthetic integration success is not the proposed end-to-end pilot exit.
GitHub main read succeeded; this session exposed no write action. No remote update is claimed.
See the packaged verification summary for actual test results and environment.
