# Changelog

## 0.8.0 · Mechanism-first / course delivery

- Require a typed mechanism for official animation export; geometry-only keyframes remain diagnostic, not a final-production loophole.
- Asset-free event/precondition/effect/invariant validation; finite conditional branches and previous-event milestone references. Sequential backend only.
- Derive image parts/state/ports/occlusion requirements before generation; bind expected effects to actual compiled state and source-copy identity.
- Persistent transfer receipt/state with explicit state artwork; no pulse-as-receipt claim.
- Per-event full/reduced-text/mechanism-only review and actual DOM/pixel-contribution visibility checks; pending never auto-approves.
- Course objectives, sources, exact claim locators, equation/unit assumptions and separate instructor-bound artifact handoff. No private inputs auto-published.
- Pin software/full raster for sparse-review vs dense-export pixel consistency; new fractional-SVG regression.
- Preserve existing audio, HTML/MP4/hybrid SVG and private source fingerprints. This is a bounded release, not universal physics, teaching-interaction or an aesthetic guarantee.


## 0.7.0

- Optional semantic performance plans compile `store/retrieve/transfer/verify/replace` into contact, state and response tracks for topic-generated artwork.
- Registered body/state/gate/front layers, normalized ports, articulated pivots, real image-state switching and foreground occlusion; clipped parts fail before export.
- Retrieval preserves stored identity and creates a distinct copy. Verify/replace results are explicit simulated teaching data, not real execution.
- Explicit SRT cue import and operation-local timing; optional local narration/music shared by HTML and MP4. No TTS, ASR, automated beat detection or lipsync.
- Shared-canvas normalization and machine-readable scoped repair guidance. Final reviews still recapture the whole timeline and never auto-approve.
- Preserve HTML/MP4/hybrid SVG, new-topic artwork policy, private receipt exclusion, old source compatibility and immutable review fingerprints.
- Add synthetic three-rig browser checks and engineering tests. No weak-model/aesthetic/cross-engine success claim.
